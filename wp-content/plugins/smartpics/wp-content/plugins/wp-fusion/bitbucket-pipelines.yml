image: composer:latest

pipelines:
  default:
    - step:
        name: Install and Run PHP CodeSniffer and PHPStan
        caches:
          - composer
        script:
          # Install PHP dependencies
          - composer install --no-interaction --prefer-dist

          # Run PHP CodeSniffer
          - composer phpcs

          # Run PHPStan
          - composer phpstan